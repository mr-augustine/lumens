using UnityEngine;
using System.Collections;

public class SweeperRenderer : MonoBehaviour {

	public GameObject sweeper;
	public Transform spawnPoint;
	
	// Use this for initialization
	void Start () {
//		sweeper = (GameObject)Instantiate (sweeper, spawnPoint.position, spawnPoint.rotation); 
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
